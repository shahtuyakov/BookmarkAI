    # Environment Variable Definitions for BookmarkAI Web Clip

    # --- OAuth Configuration ---
    # Used for authenticating users via your OAuth provider.
    # For production, these will point to your live OAuth endpoints.
    # For development, these will point to your local OAuth server (e.g., Keycloak, or a mock).
    VITE_OAUTH_AUTH_URL=https://api.bookmarkai.com/oauth/authorize
    VITE_OAUTH_TOKEN_URL=https://api.bookmarkai.com/oauth/token
    VITE_OAUTH_USERINFO_URL=https://api.bookmarkai.com/oauth/userinfo
    VITE_OAUTH_CLIENT_ID=YOUR_PRODUCTION_OAUTH_CLIENT_ID_HERE

    # --- API Configuration ---
    # Base URL for the BookmarkAI API (e.g., for fetching/creating shares).
    # Excludes specific paths like /shares, which will be appended in the code.
    VITE_API_BASE_URL=https://api.bookmarkai.com/api/v1

    # --- Web Application URL ---
    # URL for your main BookmarkAI web application, used for "View in Timeline" links.
    VITE_WEB_APP_URL=https://app.bookmarkai.com

    # --- INSTRUCTIONS FOR .env.development / .env.production ---
    #
    # 1. For local development:
    #    - Create a file named '.env.development' in this directory ('packages/extension/').
    #    - Copy the variables from this example file into '.env.development'.
    #    - Change the values to match your local development environment.
    #    Example for .env.development:
    #      VITE_OAUTH_AUTH_URL=http://localhost:3000/oauth/authorize
    #      VITE_OAUTH_TOKEN_URL=http://localhost:3000/oauth/token
    #      VITE_OAUTH_USERINFO_URL=http://localhost:3000/oauth/userinfo
    #      VITE_OAUTH_CLIENT_ID=YOUR_DEVELOPMENT_OAUTH_CLIENT_ID_HERE
    #      VITE_API_BASE_URL=http://localhost:3001/api/v1  # (e.g. if your API gateway is on 3001)
    #      VITE_WEB_APP_URL=http://localhost:8080         # (e.g. if your web app runs on 8080)
    #
    # 2. For production builds:
    #    - Create a file named '.env.production' in this directory.
    #    - Copy the variables from this example file into '.env.production'.
    #    - Ensure the values are set correctly for your production environment.
    #      (The defaults in this .env.example are geared towards production but CLIENT_ID needs setting).
    #
    # IMPORTANT:
    # - Prefix all environment variables with 'VITE_' to make them accessible in client-side code.
    # - These .env.development and .env.production files should typically NOT be committed to Git if they contain real secrets,
    #   especially if client IDs are sensitive (though often they are considered public for PKCE flows).
    #   Ensure your .gitignore file includes:
    #   .env
    #   .env.*
    #   !.env.example  (if you manage to create and commit this one)
    #   *.local